# 自己発展型ルールガバナンス

[English](../../en/dna-kernel/self-evolving-governance.md) · 日本語

## これは何か

LLM ハーネスを使うプロジェクトで、
**ルールが肥大・矛盾せず**、**完了が曖昧にならず**、**履歴が改ざんされない**ようにする運用パターンです。

「チャットで言い続ければ守られる」という運用の代わりに、
3つの構造で LLM の行動を拘束します。

## 3つの構造

### 1. 概念正本（Policy-as-Code）

`rules/concepts.md` に「短い定義・禁止・完了条件」だけを置きます。
スキルや他のルールは、その概念へのリンクと「この作業での適用」だけを書きます。

**効果**: 矛盾の発生源を1か所に集め、変更の影響範囲を管理できる。

会話言語（ホーム config）と書き込み正本の違いは [onboarding.md の「会話言語とホーム config」](onboarding.md#会話言語とホーム-config) を参照。

### 2. 完了の拘束

「完了」は、成果物がファイルに実在し、再読込または機械検証で確認が済んだ後だけと定義します。
LLM が「書きました」と言っても、ファイルへの書き込みと確認を終えるまでは完了扱いにさせません。

**効果**: 「会話に出したが保存していない」「dry-run 前に本番を走らせた」を防ぐ。

### 3. 計画書・設計書の進捗チェック

計画書・設計書には `## 進捗` を置き、進捗を `- [ ]` / `- [x]` のチェックボックスで管理します。未着手・進行中・部分完了・確認待ちは `[ ]` のまま残作業を補足し、完了した作業だけを `[x]` にします。

`## 実装フェーズ` などの詳細チェック項目も進捗の一部です。実装済みの詳細項目は `[x]`、未実施・確認待ちの項目は `[ ]` に更新し、`## 進捗` の概要と詳細の状態を一致させます。

作成・更新後は、次の機械検査を実行します。

```bash
uv run python tools/kernel/plan_check.py _workingspace/plans
```

別の設計書を検査する場合は、そのファイルまたはディレクトリを引数に渡します。終了コード0を確認してから完了扱いにします。

**効果**: セッションやPCをまたいでも、計画・設計の残作業と完了項目を同じ形式で確認できます。

計画書・設計書には、ヘッダの `バージョン: MAJOR.MINOR` と、`## 変更履歴` の追記専用テーブルも置きます。変更時はヘッダの版を更新し、日付・バージョン・変更内容を履歴の末尾へ追加します。`plan_check.py` は、版の形式、履歴表、ヘッダと最終履歴行の一致を検査します。

### 4. 追記専用の査証ログ

作業後に `_workingspace/log/YYYYMM.md` へ追記します。上書きと削除は禁止です。
`workspace_audit_log.py` を使えば、既存行を変更できないコマンドで追記できます。

**効果**: 「何をしたか」の事実が残り、「やった・やらない」の争いが起きない。

## ルールが自己発展する仕組み

1. 新しい作業パターンが出てきたら、`concepts.md` へ短い定義を追加する
2. 既存スキルや指示は、その定義へのリンクに置き換える（本文を複製しない）
3. 追加した事実を査証ログへ残す

これを繰り返すことで、ルールセットが「増やすほど薄くなる」構造になります。
概念本文は1か所に集まり、各スキルはその概念を参照するだけになっていくためです。

## LLM支援型・ユーザー統治のルール育成

ルールの整理は、ユーザーがMarkdownを毎回手書きで分類することを基本にしません。LLMが会話、実装、レビューから反復する修正・判断・プロジェクトの癖を候補として抽出し、既存ルールとの重複と適用範囲を整理します。ただし、LLMが承認なしにrulesyncへ保存することはありません。

候補を見つけたら、次を明示してユーザーへ確認します。

```text
今回の判断をrulesyncへ保存しますか？
候補: プロジェクトルール / 作業skill / 個人設定 / 今回限り（保存しない）
```

承認後だけ、`backup-before-edit` と `content-placement` に従って正本を更新し、Rulesync生成、`generate --check`、関連テスト、監査ログまで行います。却下・保留なら永続正本を変えません。「学習」はモデル再訓練ではなく、承認済み知識を正本へ保存して各targetへ再生成する意味に限定します。

## 索引と複数階層ルール

`.rulesync/rules/agents.md` は共有 `AGENTS.md` の短い入口です。詳細ルール本文を複製せず、条件、参照先、正本、完了確認だけを示します。共有入口の所有targetは一意にし、標準ルールは `targets` と `globs` で明示的に分離します。

導入時は既存ルール・skill・生成物をPythonで棚卸しし、各項目を索引のroute、常時適用、明示除外のいずれかに登録します。標準ルールが共有入口へ混入すること、target複合生成の後勝ちで内容が変わることを検査します。生成確認と、Claude Code・Codex・Cursor等が実際に読み込んだ確認は別の証跡です。

## 一時ツールと準拠プラグイン

調査や測定で一時的に必要な処理は、まずPython標準ライブラリでOSの一時領域またはGit除外済みの `_workingspace/tmp-tools/` に作成します。再利用性、入出力契約、所有者、テスト、依存性、Windows/macOS/Linux対応が確定したものだけを `tools/kernel/` へ昇格します。標準プロファイル外の任意機能は、承認後に `tools/plugins/` へ取り込みます。

昇格・プラグイン取り込みでは、manifest、target、正本境界、依存性、テスト、docs、ロールバックを確認します。単発の便利さだけで恒久化せず、未承認の依存追加や生成物の持ち込みは行いません。

## 最小カーネル（移植に必要なもの）

| 要素 | ファイル | 役割 |
|------|----------|------|
| 概念正本 | `rules/concepts.md` | 定義・禁止・完了条件の一本化 |
| ルール作法 | `rules/rule-authoring.md` | 増やし方の固定 |
| 計画書・設計書チェック | `tools/kernel/plan_check.py` | 進捗表記の機械検査 |
| 査証ログ | `tools/kernel/workspace_audit_log.py` | 追記専用の作業記録 |
| 機械検証 | `tools/kernel/novel_project_check.py` | 完了条件の自動確認 |

**最小構成**: 概念正本 + 査証ログツールだけでも効果があります。
機械検証ツールは「完了の拘束」を強化するオプションです。

## LLM 向けの入口に書くこと

`CLAUDE.md` / `AGENTS.md` に以下の2行を加えれば動きます。

```
- ルールの正本は `rules/concepts.md`（または移植先での相当パス）。変更はここを先に更新する。
- 作業後は `tools/kernel/workspace_audit_log.py` で査証ログへ追記する。
```

あとは `concepts.md` の「完了扱い条件」と「査証ログの原則」に従って動きます。
